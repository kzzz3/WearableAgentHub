# WearableAgent Hub Configuration
# ─────────────────────────────────
# This is the primary config file. Environment variables can still
# override any value (env takes priority for CI / container use).

# LLM provider settings
llm:
  provider_type: "openai_compatible"
  api_key: "tp-cmf2tb2zbvmuassajtu3sloga2sih6qfn1udjveydt43zq37"
  base_url: "https://token-plan-cn.xiaomimimo.com/v1"
  model: "mimo-v2.5-pro"

# Backend server
server:
  backend_port: 8000
  frontend_port: 5173

# A2A agent-to-agent
a2a:
  base_url: "http://localhost:8000"

# x402 payment
x402:
  base_url: "http://localhost:8002"
